{
  "name": "index",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "live-server .",
    "node-sass": "node-sass ./assets/sass/style.scss ./assets/css/style.css -w --output-style compressed",
    "dev:sass": "npm-run-all --parallel node-sass dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "sass": "^1.62.0"
  },

  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
